# Q2. Sort Vowels by Frequency

## Idea
Count the frequency of each vowel and also record the first position where each vowel appears.

Sort the vowels by:
- higher frequency first
- earlier first occurrence when frequencies are equal

Then rebuild only the vowel positions in the string using that order.

## Complexity
- Time: `O(n)`
- Space: `O(n)`
